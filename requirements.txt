pandas>=2.0
numpy>=1.26
scikit-learn>=1.3
keras>=3.0
torch>=2.9
matplotlib>=3.8
seaborn>=0.13
plotly>=5.20
streamlit>=1.35
pyyaml>=6.0
joblib>=1.3
pytest>=7.4

# Optional: if your Python version has a TensorFlow wheel, you can install
# tensorflow>=2.16 instead and set KERAS_BACKEND=tensorflow. This project
# defaults to the torch backend (see src/deephar/__init__.py) because it
# works on newer Python releases where TensorFlow does not yet publish wheels.

# Optional, only needed for scripts/download_data.py automatic download:
# kagglehub
