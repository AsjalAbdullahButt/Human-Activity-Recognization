[project]
name = "deephar"
version = "0.1.0"
description = "LSTM-based Human Activity Recognition pipeline"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
